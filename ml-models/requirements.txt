# Active dependencies for current modules in this system:
# - Handwriting OCR pipeline (Google Vision)
# - Keystroke anomaly + SHAP explainability
# Updated for Python 3.11 + SHAP 0.51.0 compatibility

# API framework
fastapi==0.108.0
uvicorn==0.25.0
pydantic==2.5.2
python-multipart==0.0.9
python-dotenv==1.0.0

# Handwriting OCR (Google Vision)
google-cloud-vision==3.7.2

# Keystroke + shared numeric stack
# numpy>=2.0 required by shap==0.51.0
# scikit-learn>=1.4.0 required for numpy 2.x compatibility
numpy>=2.0.0
joblib==1.3.2
scikit-learn>=1.4.0
shap==0.51.0
