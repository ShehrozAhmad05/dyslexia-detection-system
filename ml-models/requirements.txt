# Active dependencies for current modules in this system:
# - Handwriting OCR pipeline (Google Vision) via ml-models/main.py + handwriting/*
# - Keystroke anomaly + SHAP explainability via backend/src/ml/keystroke/*

# API framework (handwriting ML service)
fastapi==0.108.0
uvicorn==0.25.0
pydantic==2.5.2
python-multipart==0.0.9
python-dotenv==1.0.0

# Handwriting OCR (Google Vision)
google-cloud-vision==3.7.2

# Keystroke + shared numeric stack
numpy>=2.0
joblib>=1.4
scikit-learn>=1.5
shap==0.51.0