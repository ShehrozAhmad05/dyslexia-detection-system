# Active dependencies for current modules in this system:
# - Handwriting OCR pipeline (Google Vision)
# - Keystroke anomaly + SHAP explainability
# Updated for Python 3.10-3.13 compatibility

# API framework
fastapi==0.108.0
uvicorn==0.25.0
pydantic>=2.10.0,<3.0.0
python-multipart==0.0.9
python-dotenv==1.0.0

# Handwriting OCR (Google Vision)
google-cloud-vision==3.7.2

# Keystroke + shared numeric stack
# Python 3.13 requires NumPy 2.x wheels on Windows
numpy>=2.0.0,<3.0.0
joblib==1.3.2
scikit-learn>=1.5.0,<2.0.0
shap>=0.44.0,<0.50.0
